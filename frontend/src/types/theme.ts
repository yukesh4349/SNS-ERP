export interface DashboardTheme {
  isDark: boolean;
  bg: string;
  sidebarBg: string;
  cardBg: string;
  text: string;
  textMuted: string;
  border: string;
  accent: string;
  primary: string;
  success: string;
  danger: string;
}
